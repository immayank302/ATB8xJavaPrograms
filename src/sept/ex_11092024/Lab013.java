package sept.ex_11092024;

public class Lab013 {

    public static void main(String[] args) {
   // Identifiers -- variable names . Set of rules
//        boolean 123=true;   //Not possible as the variable name must not start with the number
         boolean $=true; //Possible
        boolean pramod_is_very_good=true;
       // boolean 123pramod=true; // Not possible as the variable name must not start with the number

        boolean _123=true; // It can start with _(underscore)
        System.out.println(_123);//true

        boolean PRAMOD=true; // Possible

        boolean pramod$dutta=true; // We can also use the $ sign in between

       // boolean pramod dutta=true; //not possible as the space in between is not possible



    }
}
