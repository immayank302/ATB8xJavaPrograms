package sept.ex_16092024;
// Identify what will be the output of the below program
public class Lab032 {
    public static void main(String[] args) {
      //  int enum=10; // We can't use enum as the variable name as enum is a keyword not an identifier.

      //  System.out.println(enum);
    }
}
